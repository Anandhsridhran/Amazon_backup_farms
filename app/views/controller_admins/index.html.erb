<h1>Listing controller_admins</h1>

<table>
  <tr>
    <th>Location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @controller_admins.each do |controller_admin| %>
  <tr>
    <td><%= controller_admin.location_id %></td>
    <td><%= link_to 'Show', controller_admin %></td>
    <td><%= link_to 'Edit', edit_controller_admin_path(controller_admin) %></td>
    <td><%= link_to 'Destroy', controller_admin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Controller admin', new_controller_admin_path %>
